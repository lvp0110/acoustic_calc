# Acoustic Calculator

Веб-приложение для расчета акустических материалов с выбором бренда, модели и опций.

## 🚀 Возможности

- **Выбор бренда и модели** акустических материалов
- **Настройка опций**: размеры, цвета, перфорация, кромки
- **Калькулятор** для расчета материалов по размерам или площади
- **Адаптивный дизайн** с поддержкой различных разрешений экрана
- **Экспорт результатов** в Excel
- **Таблица результатов** с детальной информацией о расчете

## 📋 Требования

- Node.js 20 или выше
- npm или yarn

## 🛠️ Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/lvp0110/acoustic_calc.git
cd acoustic_calc
```

2. Установите зависимости:
```bash
npm install
```

## 🏃 Запуск проекта

### Режим разработки

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

### Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist`

### Предпросмотр продакшен-сборки

```bash
npm run preview
```

## 📁 Структура проекта

```
src/
├── components/          # Переиспользуемые компоненты
│   ├── CalcTable.jsx    # Таблица результатов расчета
│   ├── SelectText.jsx   # Селект с текстовыми опциями
│   └── SelectWithImages.jsx  # Селект с изображениями
├── context/             # React Context
│   └── AcousticContext.jsx  # Контекст для управления состоянием
├── hooks/               # Кастомные хуки
│   └── useAcousticEngine.js
├── pages/               # Страницы приложения
│   ├── Acoustic.jsx     # Главная страница калькулятора
│   ├── CalcControls.jsx # Компонент управления расчетом
│   ├── BrandPage.jsx    # Страница выбора бренда
│   ├── ModelPage.jsx    # Страница выбора модели
│   └── OptionsPage.jsx  # Страница выбора опций
└── App.jsx              # Главный компонент приложения
```

## 🎨 Особенности интерфейса

- **Адаптивная верстка**: при разрешении более 1024px таблица результатов отображается справа от блоков выбора
- **Интуитивный выбор**: поддержка выбора с изображениями и текстовых селектов
- **Валидация данных**: проверка корректности введенных размеров и площади
- **Автоматический расчет**: автоматический расчет площади при вводе размеров

## 🌐 Деплой

Проект автоматически деплоится на GitHub Pages при каждом push в ветку `main`.

Сайт доступен по адресу: https://lvp0110.github.io/acoustic_calc/

## 🛠️ Технологии

- **React 19** - UI библиотека
- **Vite** - сборщик и dev-сервер
- **React Router** - маршрутизация
- **GitHub Actions** - CI/CD для автоматического деплоя

## 📝 Скрипты

- `npm run dev` - запуск dev-сервера
- `npm run build` - сборка для продакшена
- `npm run preview` - предпросмотр продакшен-сборки
- `npm run lint` - проверка кода линтером

## 📄 Лицензия

Этот проект является частным.

## 👤 Автор

lvp0110
